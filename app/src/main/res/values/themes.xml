<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Base theme untuk BusTrackerPassenger.
        Compose menangani semua theming sendiri lewat BusTrackerTheme {},
        jadi XML theme ini hanya sebagai "shell" agar Activity bisa launch.
        NoActionBar → kita tidak pakai ActionBar karena full Compose.
    -->
    <style name="Theme.BusTrackerPassenger" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Status bar transparan — diatur programmatically di MainActivity via enableEdgeToEdge() -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>